<!-- Kill-switches for Features -->
<FeatureAvailability>
    <!-- plugin toggle -->
    <Plugin Kill="false"/>

    <!-- all chat commands -->
    <AllCommands Kill="false"/>
    
    <!-- Chat Commands -->
    <Command Kill="true" CommandName="/refresh"/>
    <Command Kill="true" CommandName="@Everyone"/>
    <Command Kill="false" CommandName="@All"/>
    <Command Kill="false" CommandName="@RoleName"/>
    <Command Kill="false" CommandName="@PersonName"/>

    <!-- Buttons -->
    <Button Kill="true" ButtonName="Refresh"/>

    <!-- Toggle Setting -->
    <SettingToggle Kill="true" ToggleName="HideCommands"/>
    <SettingToggle Kill="true" ToggleName="Notification"/>

    <!-- Notfication System-->
    <Notification Kill="false"/>

    <!-- Debuging Dev Setting -->
    <ConsoleLogging Kill="false"/>
    
    <!-- Refresh System (Future plans) -->
    <LastRefresh Kill="false"/>
</FeatureAvailability>
