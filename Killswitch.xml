<!-- Kill-switches for Features -->
<FeatureAvailability>
    <!-- plugin toggle -->
    <Plugin Kill="false"/>

    <!-- all chat commands -->
    <AllCommands Kill="false"/>
    
    <!-- Chat Commands -->
    <Command Kill="false" CommandName="/refresh"/>
    <Command Kill="true" CommandName="@Everyone"/>
    <Command Kill="false" CommandName="@All"/>
    <Command Kill="false" CommandName="@RoleName"/>
    <Command Kill="true" CommandName="@PersonName"/>

    <!-- Buttons -->
    <Button Kill="false" ButtonName="Refresh"/>

    <!-- Toggle Setting -->
    <SettingToggle Kill="false" ToggleName="HideCommands"/>
    <SettingToggle Kill="true" ToggleName="Notification"/>

    <!-- Notfication System-->
    <Notification Kill="false"/>

    <!-- Refresh System -->
    <LastRefresh Kill="false"/>
</FeatureAvailability>
